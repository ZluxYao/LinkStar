# LinkStar 0.6.4

从 0.5.4 攒下来的一版，主要做了三件事：**把证书和反向代理做进来**、**把打洞前后那些要自己查自己抄的步骤收进来**、**手机上能用了**。

## 新增

### 证书管理

- 四种来源：上传 PEM、读本地路径、ACME DNS-01、ACME HTTP-01，另外还能自签
- 按 SNI 匹配，支持通配符，到期前自动续；续期只换指针，已经连着的连接不断
- **全新安装自带一张自签证书**。以前证书列表是空的，「挂 HTTPS」的开关勾上也用不了，握手当场失败，用户那边只看到「连不上」，联想不到是缺证书

### 内建反向代理

就是干 nginx 的活。

- 每个站点自己选监听端口、域名、要不要 HTTPS、挂哪张证书，多个端口并存
- 补 `X-Forwarded-For`，并且把 `Host` 写回客户端原本发来的那个 —— 否则 Jellyfin、Home Assistant 这类服务生成的跳转地址会变成内网 IP
- SSE 不缓冲，WebSocket 原生透传
- 后端勾错 http / https 时自动换一次重试
- 后端连不上时给一个能看懂的 502 页面，写明是哪个站点、哪个后端连不上，而不是浏览器那句 `ERR_CONNECTION_REFUSED`
- 访问日志开关

### 打洞的端口上直接挂证书

外面访问就是 https，浏览器不再有感叹号。每个服务可以配自己的域名和证书，状态在界面上看得见。另外加了 `/go/{服务名}` 跳转，端口漂了也能用名字进。

### 局域网扫描

以前「添加设备」要求你自己知道那台机器的内网 IP —— 得去路由器后台翻 DHCP 列表，或者跑去那台机器上敲命令。卡在这一步的人，后面打洞根本轮不到。

现在直接扫：列出本机接在哪几段网上，选一段扫一遍，在线的机器和它们开着的端口摆出来，点某个端口就带着 IP、端口、服务名去建服务。26 个常见端口带名字（群晖 DSM、PVE、Alist、Portainer、Jellyfin……），看见 5244 不用再去搜是什么。

### 入口重定向（Cloudflare）做成内建

原来是个 webhook 模板：得自己去 Cloudflare 后台抄三个 ID（zone / ruleset / rule）贴进 URL，再手写整段 JSON。更糟的是在后台重建一次规则，rule ID 就变了，webhook 从此静默失效，界面上还显示着旧地址。

现在三个 ID 一个都不用填，界面上只剩「入口域名 / 落地域名 / 保留路径」。同步挂在保活心跳上，地址没变不打服务商 API。保存服务时还会给落地域名补一条 DDNS 记录，家宽 IP 变了也跟得上。

### 运行日志

后台能直接看日志，按级别和关键字过滤。单日文件能到好几 MB，只从尾部读最近一段，页面上明说读了多少。

### 修改密码

「系统设置」以前是个空壳。补上这个页面，顺手修了两件事：

1. 改密码会换掉签名密钥，所有旧 token 立刻作废。以前改了等于没改，旧 token 还能再用 7 天
2. 密码长度只有前端在拦，接口谁都能直接调，一位密码也能设进去。下限挪到服务端，并从 6 提到 8

## 改进

- **手机端**。后台侧栏改成抽屉，表格给最小宽度，中文不再被挤成一列一个字；主页一行 4 列，上下滑动改成左右翻页，按钮收到底部一排。14 处弹窗在手机上比屏幕还宽（375px 的手机上弹窗实测 720px，一半输入框在屏幕外面），一起修了
- **主页壁纸**。默认背景换成纯 CSS 的淡色渐变，不走网络，第一帧就有画面；支持自己上传壁纸；默认背景的 CSS 可以自己改。壁纸上的文字和按钮配色跟着背景深浅走
- **服务列表**。加了「复制服务」，照着现有的开一份新的；启用 / 停用开关挪到卡片上，不用再进编辑弹窗
- **DDNS**。IP 来源新增「自定义」；「本地网卡」来源给出网卡列表，看着地址挑，不用背网卡名
- **Cloudflare 规则名带上服务名**。以前后台看到一排 `linkstar:1-2` 根本认不出是哪个服务，现在是 `linkstar:1-2 | 群晖`
- 顶栏去掉搜索框、铃铛、帮助 —— 三个点了没反应的假控件
- 后台能点回导航主页了：顶栏右上角按钮，或者点侧边栏的 logo
- 桌面端默认窗口开到 1440x900，并且会按主显示器可用区域压一下，不会在小屏笔记本上跑出屏幕外
- 仪表盘按真实接口重写，删掉 mock 数据和编造的面板
- ACME 弹窗补两句提示：staging 签出来的证书浏览器不认；正式环境一小时失败 5 次会被挡住

## 修复

- **入口重定向写着「成功 → 旧端口」，实际是断的**。洞重开换了外网端口，界面上方的地址立刻跟着变了，底下「最近一次同步」却还是绿色的「成功」配着上一个端口 —— 而 Cloudflare 那边此刻真就还是旧端口，从入口域名进来一打一个空。看着一切正常，实际断着，最难查的就是这种。

  原因是两件事的时机不一样：端口在洞刚映射出来时就知道了，可规则要等洞被外面拨回来确认通了才敢写。现在这段空窗期会明说「外网端口已经换了，现在从入口域名进来打不开」，通了自己改过去。顺带修了另外两处：手点「立即同步」的结果现在也记进那一行了（以前点完还显示着上一次自动同步的地址）；规则正在写、还没有结果的时候不再提前报成功。
- **开机之后 DDNS 要等五分钟才同步上**。DDNS 和打洞是并排启动的，DDNS 先跑那一轮时打洞多半还在挑服务器，取不到公网 IP 只能记一次失败 —— 然后就按正常间隔（默认 5 分钟）干等下一轮，这期间域名一直指着上一个地址。

  现在公网 IP 一拿到就回头触发一次同步，不用等。另外没同步成的记录改成先隔 30 秒再试，每失败一次翻一倍，最多退回到配置的间隔 —— 开机这种「还没轮到」的很快就能补上，而 token 填错这种「一直就是错的」也不会变成每 15 秒去敲一次服务商的 API。
- **地址栏敲「域名:端口」打不开，而且标「不安全」**。浏览器对 `域名:端口` 默认按 http 发，而那个端口上等的是 https：握手当场失败，连接被一声不吭地掐断。浏览器拿到的是 `ERR_EMPTY_RESPONSE`，显示的是一个错误页 —— 错误页开在 `http://` 地址上，所以地址栏是红色的「不安全」，可点开证书面板又写着「证书有效」。两句话互相打架，照着哪句查都查不出来。

  现在明文 HTTP 打到 HTTPS 端口上，会回一个 302 把浏览器送到同一个地址的 https 上，输错 scheme 直接就进去了。打洞的洞口和内建反向代理两条路都补了。不是 HTTP 也不是 TLS 的客户端（端口扫描之类）照旧什么都不回，不给它的报错添乱。
- **桌面端拖到另一块屏幕后界面缩成一半**。两块屏 DPI 不一样时，把窗口从 100% 的屏拖到 150% 的屏，网页内容缩到一半，拖回来也不恢复，只能重启。原因在 wails 依赖的 go-webview2 把一个 double 参数按指针传了，升级 wails v3 到 beta.23 修掉
- **STUN 页面设备列表那一栏挤成一团**，标题被劈成两行。另外服务卡片是窗口越大反而越窄（超过 1280 硬切三列，卡片宽度从 380px 掉到 250px，服务名被截成「Li...」），改成按卡片最小宽度自动排列
- **删服务时留下一堆孤儿**：自动加的 DNS 解析记录、Cloudflare 规则、入口域名的占位记录，以前都不跟着删。留下的记录会继续往服务商推 IP，维护一个早就没有服务在听的域名，两头都不报错。现在会一起收走，三道保险：没打自动标记的不动、用户自己改过的不动、还有别的服务在用同一个域名的不动
- **复制服务时 webhook 默认关着**。照抄一份的 webhook URL 常常指着一条具体的记录或规则，两个洞轮流往同一条上写，两边都显示「发送成功」，可那个域名任何时刻只能通到其中一个，另一个是断的，还看不出来是断的

## 升级注意

- 密码长度下限从 6 提到 8，只影响新设和修改密码，老密码照常能登
- 改完密码，其他设备要重新登录
- Cloudflare 规则名格式变了。升级上来之后下次同步会就地把名字换掉，不会多出重复的规则
- 自签证书只在配置文件第一次被创建出来时垫一张。已有配置不受影响；删掉之后重启也不会再长回来

## 下载

| 文件 | 平台 |
| --- | --- |
| `linkstar` | Linux x86_64 服务端 |
| `linkstar-linux-arm64` | Linux ARM64（树莓派 4/5、ARM 软路由、NAS） |
| `linkstar-linux-armv7` | Linux ARMv7（老树莓派、32 位 ARM 路由器） |
| `linkstar-linux-mipsle` | Linux MIPS 小端（OpenWrt 路由器） |
| `linkstar-cli.exe` | Windows x86_64 命令行服务端 |
| `linkstar-desktop.exe` | Windows x86_64 桌面版，带界面和托盘 |
| `LinkStar-0.6.4-x86.fpk` | 飞牛 NAS（fnOS）安装包 |

macOS 版本需要在 Mac 上自行编译。

**完整改动**: https://github.com/ZluxYao/LinkStar/compare/LinkStar-0.5.4...LinkStar-0.6.4
